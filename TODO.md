# Task: Header Redesign - Transparent, Dynamic, Animated

## Goals
- Make header transparent so hero image is visible through it
- Add scroll-aware behavior (transparent at top, blurred/solid when scrolled)
- Add nice animations (nav entrance, hover underlines, dropdown, mobile menu)
- Don't overshadow the header design

## Steps
- [x] Rewrite `Header.tsx` with scroll-aware transparency, animated dropdown, animated mobile menu, and nav link entrance animations
- [x] Add keyframe animations to `index.css` for nav links, dropdown, and mobile menu
- [x] Verify the header hover/scroll behavior and animations work correctly
